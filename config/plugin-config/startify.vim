"" Startify
"" ---------------------------------------------------------------------------

let g:startify_custom_header = map(split(system('figlet -f  rozzo "DOOM"'), '\n'), '"   ". v:val') + ['','']
