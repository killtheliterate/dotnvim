"" Startify
"" ---------------------------------------------------------------------------

let g:startify_custom_header = map(split(system('figlet -f banner "the drear light of zoo"'), '\n'), '"   ". v:val') + ['','']
